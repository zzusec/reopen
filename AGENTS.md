# Repository guidelines

- Prefix commit messages with a type, for example `feat:`, `fix:`, `ci:`, or `chore:`.
- Before creating or moving a release tag, update `buildinfo.Version` in `internal/buildinfo/buildinfo.go` so it exactly matches the tag without the leading `v`.
